# SYSTEMD unit file.

[Unit]
Description=rcap: Packet Dump Utility
After=network.target

[Service]
Type=simple
ExecStart=/usr/local/sbin/rcap -c /etc/rcap.toml
ExecReload=/bin/kill -SIGHUP ${MAINPID}
KillSignal=SIGINT
Restart=on-failure

[Install]
WantedBy=multi-user.target
